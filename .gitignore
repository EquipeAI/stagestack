.DS_Store
.env
.env.*
!.env.example
node_modules/
.output/
dist/
.vercel/
*.log
# Walkthrough videos are too large for git (~35MB each); keep local copies only
walkthrough-*.mp4
# Verification screenshots from UI passes; regenerate locally rather than
# carrying ~200KB PNGs per run in git (same reasoning as the walkthrough videos)
.perf-research/*-preview/
